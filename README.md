# ft_printf

A custom implementation of the C printf function, built as part of the 42 London curriculum. Handles the following format specifiers: %c, %s, %p, %d, %i, %u, %x, %X, and %%.

## Usage

Run make, then include ft_printf.h in your project and link with the compiled library.
